import SignoutForm from '../../../components/auth/signout/Signout';

export default function LoginPage() {
  return (
    <div className="w-full h-screen flex justify-center items-center bg-gray-900">
      <SignoutForm />
    </div>
  );
}